{
    "name": "x-dttm-customer-service-extension",
    "version": "1.0.0",
    "description": "Extends the standard Customer Service application to add custom effort tracking fields and access controls to cases.",
    "license": "UNLICENSED",
    "imports": {
        "#now:*": "./@types/servicenow/fluent/*/index.js"
    },
    "scripts": {
        "build": "now-sdk build",
        "deploy": "now-sdk install",
        "transform": "now-sdk transform",
        "types": "now-sdk dependencies"
    },
    "devDependencies": {
        "@servicenow/sdk": "4.5.0",
        "@servicenow/glide": "27.0.4",
        "eslint": "8.50.0",
        "@servicenow/eslint-plugin-sdk-app-plugin": "4.5.0"
    }
}
